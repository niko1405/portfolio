[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Public Vite client-side values intentionally exposed in bundle.
  SECRETS_SCAN_OMIT_KEYS = "VITE_EMAILJS_SERVICE_ID,VITE_EMAILJS_TEMPLATE_ID,VITE_EMAILJS_PUBLIC_KEY"
